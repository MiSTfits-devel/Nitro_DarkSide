diff --git a/src/ARM.cpp b/src/ARM.cpp
index 827fcb01..bbb2f72f 100644
--- a/src/ARM.cpp
+++ b/src/ARM.cpp
@@ -589,6 +589,23 @@ void ARM::CheckGdbIncoming()
     GdbCheckA();
 }
 
+// --- NDS_MiSTfits differential-trace hook (see docs/TRACE_DIFF.md there) ---
+// One line per retired instruction: <pc> <opcode> <cpsr> <r0>..<r14>.
+// pc is the pipeline value captured before execution (addr+8 ARM, +4 Thumb),
+// cpsr/regs are post-execute — matching the RTL export in nds_cpu9.
+FILE* ARM9TraceFile = nullptr;
+u64 ARM9TraceCount = 0;
+u64 ARM9TraceMax = 0;
+
+static inline void ARM9TraceLine(u32 pc, u32 instr, u32 cpsr, u32* r)
+{
+    if (!ARM9TraceFile || ARM9TraceCount >= ARM9TraceMax) return;
+    fprintf(ARM9TraceFile, "%08x %08x %08x", pc, instr, cpsr);
+    for (int i = 0; i < 15; i++) fprintf(ARM9TraceFile, " %08x", r[i]);
+    fputc('\n', ARM9TraceFile);
+    ARM9TraceCount++;
+}
+
 template <CPUExecuteMode mode>
 void ARMv5::Execute()
 {
@@ -670,8 +687,10 @@ void ARMv5::Execute()
                 else             NextInstr[1] = CodeRead32(R[15], false);
 
                 // actually execute
+                u32 trace_pc = R[15];
                 u32 icode = (CurInstr >> 6) & 0x3FF;
                 ARMInterpreter::THUMBInstrTable[icode](this);
+                ARM9TraceLine(trace_pc, CurInstr & 0xFFFF, CPSR, R);
             }
             else
             {
@@ -685,6 +704,7 @@ void ARMv5::Execute()
                 NextInstr[1] = CodeRead32(R[15], false);
 
                 // actually execute
+                u32 trace_pc = R[15];
                 if (CheckCondition(CurInstr >> 28))
                 {
                     u32 icode = ((CurInstr >> 4) & 0xF) | ((CurInstr >> 16) & 0xFF0);
@@ -696,6 +716,7 @@ void ARMv5::Execute()
                 }
                 else
                     AddCycles_C();
+                ARM9TraceLine(trace_pc, CurInstr, CPSR, R);
             }
 
             // TODO optimize this shit!!!
diff --git a/src/ARMInterpreter.cpp b/src/ARMInterpreter.cpp
index 80b64dea..3f48f7d0 100644
--- a/src/ARMInterpreter.cpp
+++ b/src/ARMInterpreter.cpp
@@ -40,7 +40,7 @@ void A_UNK(ARM* cpu)
 #ifdef GDBSTUB_ENABLED
     cpu->GdbStub.Enter(cpu->GdbStub.IsConnected(), Gdb::TgtStatus::FaultInsn, cpu->R[15]-8);
 #endif
-    //for (int i = 0; i < 16; i++) printf("R%d: %08X\n", i, cpu->R[i]);
+    for (int i = 0; i < 16; i++) printf("R%d: %08X\n", i, cpu->R[i]);
     //NDS::Halt();
     u32 oldcpsr = cpu->CPSR;
     cpu->CPSR &= ~0xBF;
diff --git a/src/ARM_InstrTable.h b/src/ARM_InstrTable.h
index 13d67bff..ec60e2f5 100644
--- a/src/ARM_InstrTable.h
+++ b/src/ARM_InstrTable.h
@@ -39,7 +39,7 @@ INSTRFUNC_PROTO(ARMInstrTable[4096]) =
     // 0000 0011 0000
     A_EOR_REG_LSL_IMM_S, A_EOR_REG_LSL_REG_S, A_EOR_REG_LSR_IMM_S, A_EOR_REG_LSR_REG_S,
     A_EOR_REG_ASR_IMM_S, A_EOR_REG_ASR_REG_S, A_EOR_REG_ROR_IMM_S, A_EOR_REG_ROR_REG_S,
-    A_EOR_REG_LSL_IMM_S, A_MLA, A_EOR_REG_ROR_IMM_S, A_UNK,
+    A_EOR_REG_LSL_IMM_S, A_MLA, A_EOR_REG_LSR_IMM_S, A_UNK, // was ROR_IMM_S: table typo, EORS ...,LSR #odd ran as ROR
     A_EOR_REG_ASR_IMM_S, A_UNK, A_EOR_REG_ROR_IMM_S, A_UNK,
 
     // 0000 0100 0000
